<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.danielaluciano.teachersjob">

    <uses-permission android:name="android.permission.CAMERA" android:requiredFeature="true">
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CadastrarLoginActivity"
            android:label="Cadastrar-se"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity android:name=".MuralDeVagasActivity"
            android:label="Mural de Vagas"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity android:name=".TermosCondicoesActivity"
            android:label="Termos e Condições"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity android:name=".AreaAtuacaoActivity"
            android:label="Área de Atuação"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>


    </application>

</manifest>